<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1A1A1A"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="XIOTTY DLC"
        android:textColor="#00FFCC"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_gravity="center"
        android:layout_marginTop="32dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Standleo Private v3.4"
        android:textColor="#888888"
        android:layout_gravity="center"
        android:layout_marginBottom="32dp"/>

    <!-- ESP Section -->
    <TextView android:text="ESP" android:textColor="#00FFCC" android:textSize="18sp" android:layout_marginTop="16dp"/>
    <Switch android:id="@+id/switchBoxESP" android:text="Box ESP" android:textColor="#FFFFFF"/>
    <Switch android:id="@+id/switchSnaplines" android:text="Snaplines" android:textColor="#FFFFFF"/>
    <Switch android:id="@+id/switchDistance" android:text="Distance ESP" android:textColor="#FFFFFF"/>

    <!-- Aimbot Section -->
    <TextView android:text="AIMBOT" android:textColor="#00FFCC" android:textSize="18sp" android:layout_marginTop="16dp"/>
    <Switch android:id="@+id/switchSilentAim" android:text="Silent Aim" android:textColor="#FFFFFF"/>
    <Switch android:id="@+id/switchAutoFire" android:text="Auto Fire" android:textColor="#FFFFFF"/>
    <LinearLayout android:orientation="horizontal" android:layout_width="match_parent" android:layout_height="wrap_content">
        <TextView android:text="FOV:" android:textColor="#FFFFFF"/>
        <SeekBar android:id="@+id/seekFOV" android:layout_width="200dp" android:layout_height="wrap_content" android:max="360"/>
        <TextView android:id="@+id/txtFOVValue" android:text="90" android:textColor="#00FFCC"/>
    </LinearLayout>

    <!-- Chams Section -->
    <TextView android:text="CHAMS" android:textColor="#00FFCC" android:textSize="18sp" android:layout_marginTop="16dp"/>
    <Switch android:id="@+/id/switchChams" android:text="Rainbow Chams" android:textColor="#FFFFFF"/>

    <!-- BunnyHop -->
    <Switch android:id="@+id/switchBunnyHop" android:text="BunnyHop" android:textColor="#FFFFFF" android:layout_marginTop="16dp"/>

    <!-- Inject Button -->
    <Button
        android:id="@+id/btnInject"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="INJECT XIOTTY DLC"
        android:backgroundTint="#00FFCC"
        android:textColor="#1A1A1A"
        android:textSize="18sp"
        android:layout_marginTop="32dp"/>

    <TextView
        android:id="@+id/txtStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Status: Waiting for Shizuku"
        android:textColor="#FF5555"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"/>
</LinearLayout>
